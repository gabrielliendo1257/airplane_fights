spring:
  datasource:
    username: admin
    password: admin123
    url: jdbc:h2:mem:authorization_service_dev;DB_CLOSE_DELAY=-1;DB_CLOSE_ON_EXIT=FALSE

  h2:
    console:
      enabled: true

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: create-drop
    defer-datasource-initialization: true


eureka:
  client:
    service-url:
      defaultZone: http://127.0.0.1:8761/eureka

logging:
  level:
    root: info
    org.springframework.web: debug
    org.springframework.security: debug
    org.springframework.web.servlet.mvc.method.annotation.RequestMappingHandlerMapping: trace
    org.springframework.security.oauth2: debug
    org.springframework.data.redis: debug
    jakarta.jms: debug

authorization:
  env:
    oauth2:
      registration-id: "movie-app"
      client-id: "movie-bff"
      client-password: "super-secret"
      redirect: "lb://bff-web-client/login/oauth2/code/flight"
      logout-redirect: http://127.0.0.1:4200/home
    # jwk:
    #   uri: "lb://authorization-service/oauth2/jwks"
    api:
      path-base: /api/v1/authorization

server:
  host: 127.0.0.1
  port: 9090
